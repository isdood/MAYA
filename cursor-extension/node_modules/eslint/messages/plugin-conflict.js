@pattern_meta@
GLIMMER Pattern:
{
  "metadata": {
    "timestamp": "2025-06-16 08:36:04",
    "author": "isdood",
    "pattern_version": "1.0.0",
    "color": "#FF69B4"
  },
  "file_info": {
    "path": "./cursor-extension/node_modules/eslint/messages/plugin-conflict.js",
    "type": "js",
    "hash": "89edde8dfa81a17df906936ab734945b5da7e6ec"
  }
}
@pattern_meta@

"use strict";

module.exports = function(it) {
    const { pluginId, plugins } = it;

    let result = `ESLint couldn't determine the plugin "${pluginId}" uniquely.
`;

    for (const { filePath, importerName } of plugins) {
        result += `
- ${filePath} (loaded in "${importerName}")`;
    }

    result += `

Please remove the "plugins" setting from either config or remove either plugin installation.

If you still can't figure out the problem, please stop by https://eslint.org/chat/help to chat with the team.
`;

    return result;
};
